# Generated by `npm run build:data` (scripts/build_data.mjs). Formatting is
# owned by scripts/format_data.mjs so the bake is byte-stable; oxfmt must not
# reformat them (lint-staged runs oxfmt on src/**).
src/data/
